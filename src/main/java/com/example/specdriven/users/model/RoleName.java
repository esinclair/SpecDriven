package com.example.specdriven.users.model;

/**
 * Predefined access control roles for the system.
 * These roles correspond to the RoleName enum in the OpenAPI specification.
 */
public enum RoleName {
    ADMIN,
    USER,
    AUDITOR
}
