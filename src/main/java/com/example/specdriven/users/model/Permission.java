package com.example.specdriven.users.model;

/**
 * Granular permissions that can be assigned to roles.
 * These permissions control access to specific operations.
 */
public enum Permission {
    USER_READ,
    USER_WRITE,
    ROLE_ASSIGN
}
